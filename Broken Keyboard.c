#include<stdio.h>
#include<string.h>
int main()
{
    int b;
    scanf("%d", &b);
    char s[101];
    scanf("%s", s);

    for(int x=0; x<b; x++)
    {
        int flag=0;
        int n;
        scanf("%d", &n);
        char str[n];
        scanf("%s", str);
    }
        return 0;
}
